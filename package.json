{
  "name": "@lo/eslint-config-monorepo",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "author": "LoTwT <709937065@qq.com> (https://github.com/LoTwT/)",
  "scripts": {
    "lint": "eslint --fix .",
    "prepare": "pnpm -r run stub",
    "release": "bumpp package.json packages/*/package.json --commit --push --tag && pnpm -r publish --access public",
    "pub": "pnpm -r publish --access public"
  },
  "devDependencies": {
    "@lo/eslint-config": "workspace:*",
    "bumpp": "^7.1.1",
    "eslint": "^8.13.0",
    "eslint-plugin-lo": "workspace:*",
    "typescript": "^4.6.3"
  }
}